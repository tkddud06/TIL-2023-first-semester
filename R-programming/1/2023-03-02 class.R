# 2023-03-02 내용 정리

for (i in seq(1,10)){
	print(i)
}

a = matrix(rep(1,16),nrow=4)

# seq(시작from, 끝to, 필요시 by=간격, 필요시 length.out=결과갯수(맞춰서 일정간격으로 뽑아줌) :
# 파이썬의 range같은 친구. 1:10과 같은 식ㄱ으로도 작성할 수 있음.(1~10까지)
# c(내용) 콤바인, 벡터 정의할때 쓰는 함수
# matrix, for문, print 등.
# rep(내용, 횟수) : 반복.
# ?를 명령어 앞에 쓰면, 도움말 볼 수 있다.
